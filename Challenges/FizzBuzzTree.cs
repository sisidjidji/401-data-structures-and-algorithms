using System;
using System.Collections.Generic;
using System.Text;
using Data_Structures.tree;

namespace Challenges
{
    public class FizzBuzzTree<T>
    {
        public Node<T> Root;
       // public BinaryTree<string> FizzBuzz (Node<string> node)
        //{
        //    BinaryTree<string> tree = new BinaryTree<string>();

        //    tree.PreOrder(node);

        //    if (Convert.ToInt32(node.Value) % 3 == 0 && Convert.ToInt32(node.Value) % 5==0)
        //    {
        //        node.Value = "FizzBuzz";
                
        //    }
        //    if (Convert.ToInt32(node.Value) % 3 == 0 )
        //    {
        //        node.Value = "Fizz";
        //    }
        //    if (Convert.ToInt32(node.Value) % 3 == 0 && Convert.ToInt32(node.Value) % 5 == 0)
        //    {
        //        node.Value = "Buzz";
        //    }

        //    return tree;
        //}

    }
}
